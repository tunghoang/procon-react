# Team manager (Express). Data routes are mounted under /api, so the base URL
# must include the /api prefix (pages append e.g. /team, /match).
VITE_SERVICE_API = https://procon25-api.haiuet.me/api
# HEXUDON game service (FastAPI), including the /api prefix.
VITE_GAME_SERVICE_API = http://127.0.0.1:8001/api
